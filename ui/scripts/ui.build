rm -rf build
mkdir build
yarn esbuild {UI_ESBUILD_ARGS} ui/app.tsx --loader:.ts=tsx --loader:.js=jsx "--external:fs" --bundle --sourcemap --outfile=build/ui.js
cp ui/index.html build
cp ui/style.css build